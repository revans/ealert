#!/usr/bin/env ruby -w 

require 'rubygems'
require 'ealert'

if ARGV.first.nil?
  puts "What event do you want to stream?"
else
  keywords = ARGV.join(', ')
  puts "Going to stream: #{keywords}"
  ::EAlert::TwitterFilter.by_keywords(keywords, ['revans', 'g$ther*'])
end